import { Button, ButtonProps } from '@mantine/core'

// ----------------------------------------------------------------------

export const TestButton = (props: ButtonProps) => <Button {...props} />
